#ifndef CREATEOBJ_HPP
#define CREATEOBJ_HPP

#include "parabolicmovi.hpp"

Object init_projectile(double mass, double height, double initAng, double initvel, double denair);

Object init_target(double mass);

#endif // CREATEOBJ_HP
